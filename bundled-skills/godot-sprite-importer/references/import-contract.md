# Import Contract

A sprite pack is easiest to import when it has:

- `spritesheet.png` or `spritesheet.webp`
- `frames/frames-manifest.json`
- separate extracted frames when possible
- clear animation or state names
- cell size and atlas coordinates
